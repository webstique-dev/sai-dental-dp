:root {
  --color-primary: #0f766e;
  --color-primary-dark: #115e59;
  --color-primary-light: #14b8a6;
  --color-primary-soft: #ccfbf1;
  --color-accent: #0ea5e9;
  --color-success: #16a34a;
  --color-danger: #dc2626;
  --color-amber: #d97706;
  --color-text: #0f172a;
  --color-text-muted: #64748b;
  --color-border: #e2e8f0;
  --color-bg: #ffffff;
  --color-bg-soft: #f8fafc;
  --color-bg-subtle: #f1f5f9;
  --radius-sm: 8px;
  --radius-md: 12px;
  --radius-lg: 16px;
  --radius-xl: 24px;
  --shadow-sm: 0 1px 2px rgb(15 23 42 / 0.06);
  --shadow-md: 0 4px 12px rgb(15 23 42 / 0.08);
  --shadow-lg: 0 12px 32px rgb(15 23 42 / 0.12);
  --font-sans: 'Segoe UI', system-ui, -apple-system, 'Helvetica Neue', Arial,
    sans-serif;
  --header-height: 72px;
  --container-max: 1200px;
}

*,
*::before,
*::after {
  box-sizing: border-box;
}

html,
body,
#root {
  margin: 0;
  min-height: 100vh;
}

body {
  font-family: var(--font-sans);
  color: var(--color-text);
  background: var(--color-bg);
  line-height: 1.6;
  -webkit-font-smoothing: antialiased;
  text-rendering: optimizeLegibility;
}

h1,
h2,
h3,
h4,
p {
  margin: 0;
}

a {
  color: var(--color-primary);
  text-decoration: none;
}

a:hover {
  text-decoration: underline;
}

.container {
  width: 100%;
  max-width: var(--container-max);
  margin: 0 auto;
  padding: 0 24px;
}

.btn {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  gap: 8px;
  border-radius: 999px;
  padding: 12px 24px;
  font-size: 15px;
  font-weight: 600;
  font-family: inherit;
  border: 1px solid transparent;
  cursor: pointer;
  transition:
    background 0.15s ease,
    box-shadow 0.15s ease,
    transform 0.15s ease;
}

.btn:focus-visible {
  outline: 3px solid var(--color-primary-light);
  outline-offset: 2px;
}

.btn-primary {
  background: var(--color-primary);
  color: #fff;
}

.btn-primary:hover {
  background: var(--color-primary-dark);
  text-decoration: none;
  box-shadow: var(--shadow-md);
}

.btn-outline {
  border-color: var(--color-border);
  background: var(--color-bg);
  color: var(--color-text);
}

.btn-outline:hover {
  border-color: var(--color-primary);
  color: var(--color-primary);
  text-decoration: none;
}

.btn-secondary {
  background: var(--color-bg-subtle);
  color: var(--color-text);
}

.btn-secondary:hover {
  background: var(--color-border);
  text-decoration: none;
}

/* ---------- App shell ---------- */

.app-shell {
  min-height: 100vh;
  display: flex;
  flex-direction: column;
}

.site-header {
  position: sticky;
  top: 0;
  z-index: 20;
  background: rgb(255 255 255 / 0.9);
  backdrop-filter: blur(8px);
  border-bottom: 1px solid var(--color-border);
}

.header-inner {
  height: var(--header-height);
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 24px;
}

.brand {
  display: inline-flex;
  align-items: center;
  gap: 12px;
  color: var(--color-text);
  font-weight: 700;
  font-size: 20px;
}

.brand:hover {
  text-decoration: none;
}

.brand-mark {
  width: 40px;
  height: 40px;
  display: grid;
  place-items: center;
  border-radius: var(--radius-md);
  background: linear-gradient(135deg, var(--color-primary), var(--color-primary-light));
  color: #fff;
  font-size: 20px;
  font-weight: 800;
}

.brand-name span {
  color: var(--color-primary);
}

.site-nav {
  display: flex;
  align-items: center;
  gap: 4px;
}

.site-nav a {
  padding: 8px 14px;
  border-radius: 999px;
  color: var(--color-text-muted);
  font-size: 15px;
  font-weight: 500;
}

.site-nav a:hover {
  color: var(--color-primary);
  background: var(--color-bg-subtle);
  text-decoration: none;
}

.site-main {
  flex: 1;
  padding: 0;
}

.site-footer {
  border-top: 1px solid var(--color-border);
  padding: 28px 0;
  background: var(--color-bg-soft);
  color: var(--color-text-muted);
  font-size: 14px;
}

/* ---------- Landing page ---------- */

.hero {
  display: grid;
  grid-template-columns: 1.2fr 0.8fr;
  gap: 48px;
  align-items: center;
}

.hero-content {
  max-width: 560px;
}

.eyebrow {
  display: inline-block;
  background: var(--color-primary-soft);
  color: var(--color-primary-dark);
  font-size: 13px;
  font-weight: 700;
  letter-spacing: 0.04em;
  text-transform: uppercase;
  padding: 6px 14px;
  border-radius: 999px;
  margin-bottom: 20px;
}

.hero-content h1 {
  font-size: clamp(32px, 5vw, 52px);
  line-height: 1.1;
  letter-spacing: -0.02em;
  margin-bottom: 20px;
}

.hero-content p {
  font-size: 18px;
  color: var(--color-text-muted);
  margin-bottom: 32px;
}

.hero-actions {
  display: flex;
  flex-wrap: wrap;
  gap: 12px;
}

.hero-card {
  background: var(--color-bg-soft);
  border: 1px solid var(--color-border);
  border-radius: var(--radius-xl);
  padding: 28px;
  box-shadow: var(--shadow-lg);
}

.health-card {
  display: flex;
  flex-direction: column;
  gap: 16px;
}

.health-card-head {
  display: flex;
  align-items: center;
  gap: 10px;
}

.health-title {
  font-weight: 700;
  font-size: 16px;
}

.health-dot {
  width: 10px;
  height: 10px;
  border-radius: 50%;
  background: var(--color-text-muted);
}

.health-success .health-dot {
  background: var(--color-success);
  box-shadow: 0 0 0 4px rgb(22 163 74 / 0.15);
}

.health-error .health-dot {
  background: var(--color-danger);
  box-shadow: 0 0 0 4px rgb(220 38 38 / 0.15);
}

.health-loading .health-dot {
  background: var(--color-amber);
  animation: pulse 1.2s ease-in-out infinite;
}

.health-message {
  color: var(--color-text-muted);
}

.health-message.health-ok {
  color: var(--color-success);
  font-weight: 600;
}

@keyframes pulse {
  0%,
  100% {
    opacity: 1;
  }
  50% {
    opacity: 0.35;
  }
}

.btn-block {
  width: 100%;
}

.btn-sm {
  padding: 8px 14px;
  font-size: 14px;
}

.avatar {
  width: 38px;
  height: 38px;
  display: grid;
  place-items: center;
  border-radius: 50%;
  background: var(--color-primary-soft);
  color: var(--color-primary-dark);
  font-weight: 700;
}

/* ---------- Forms ---------- */

.field {
  display: flex;
  flex-direction: column;
  gap: 6px;
  margin-bottom: 16px;
}

.field-label {
  font-size: 14px;
  font-weight: 600;
  color: var(--color-text);
}

.field input,
.field select,
.field textarea {
  font: inherit;
  padding: 12px 14px;
  border: 1px solid var(--color-border);
  border-radius: var(--radius-sm);
  background: var(--color-bg);
  color: var(--color-text);
  transition: border-color 0.15s, box-shadow 0.15s;
}

.field input:focus,
.field select:focus,
.field textarea:focus {
  outline: none;
  border-color: var(--color-primary);
  box-shadow: 0 0 0 3px rgb(15 118 110 / 0.15);
}

.form-error {
  color: var(--color-danger);
  background: rgb(220 38 38 / 0.08);
  border: 1px solid rgb(220 38 38 / 0.25);
  border-radius: var(--radius-sm);
  padding: 10px 12px;
  font-size: 14px;
  margin-bottom: 16px;
}

/* ---------- Login page ---------- */

.login-page {
  min-height: 100vh;
  display: grid;
  place-items: center;
  padding: 32px 16px;
  background:
    radial-gradient(60% 50% at 80% 0%, var(--color-primary-soft) 0%, transparent 60%),
    var(--color-bg-soft);
}

.login-card {
  width: 100%;
  max-width: 400px;
  background: var(--color-bg);
  border: 1px solid var(--color-border);
  border-radius: var(--radius-xl);
  padding: 36px;
  box-shadow: var(--shadow-lg);
}

.login-card .brand {
  margin-bottom: 24px;
}

.login-title {
  font-size: 24px;
  margin-bottom: 4px;
}

.login-sub {
  color: var(--color-text-muted);
  margin-bottom: 24px;
}

.login-back {
  margin-top: 20px;
  text-align: center;
  font-size: 14px;
}

/* ---------- Portal layout ---------- */

.portal {
  min-height: 100vh;
  display: flex;
  background: var(--color-bg-soft);
}

.portal-sidebar {
  width: 260px;
  flex-shrink: 0;
  background: var(--color-bg);
  border-right: 1px solid var(--color-border);
  display: flex;
  flex-direction: column;
  position: sticky;
  top: 0;
  height: 100vh;
  overflow-y: auto;
}

.sidebar-header {
  padding: 20px;
  display: flex;
  align-items: center;
  justify-content: space-between;
  border-bottom: 1px solid var(--color-border);
}

.sidebar-close {
  display: none;
  background: none;
  border: none;
  font-size: 28px;
  line-height: 1;
  color: var(--color-text-muted);
  cursor: pointer;
}

.sidebar-nav {
  padding: 12px;
}

.nav-group {
  margin-bottom: 8px;
}

.nav-section-label {
  font-size: 11px;
  font-weight: 700;
  letter-spacing: 0.08em;
  text-transform: uppercase;
  color: var(--color-text-muted);
  padding: 8px 12px;
}

.nav-link {
  display: block;
  padding: 10px 12px;
  border-radius: var(--radius-sm);
  color: var(--color-text-muted);
  font-size: 15px;
  font-weight: 500;
}

.nav-link:hover {
  background: var(--color-bg-subtle);
  color: var(--color-primary);
  text-decoration: none;
}

.nav-link.is-active {
  background: var(--color-primary-soft);
  color: var(--color-primary-dark);
  font-weight: 600;
}

.portal-main {
  flex: 1;
  min-width: 0;
  display: flex;
  flex-direction: column;
}

.portal-topbar {
  background: var(--color-bg);
  border-bottom: 1px solid var(--color-border);
  padding: 12px 24px;
  display: flex;
  align-items: center;
  gap: 16px;
  justify-content: space-between;
  position: sticky;
  top: 0;
  z-index: 10;
}

.menu-toggle {
  display: none;
  background: var(--color-bg-subtle);
  border: 1px solid var(--color-border);
  border-radius: var(--radius-sm);
  padding: 8px 12px;
  font-size: 18px;
  cursor: pointer;
}

.portal-user {
  display: flex;
  align-items: center;
  gap: 12px;
  margin-left: auto;
}

.portal-user-meta {
  display: flex;
  flex-direction: column;
  line-height: 1.2;
  text-align: right;
}

.portal-user-name {
  font-weight: 600;
  font-size: 14px;
}

.portal-user-role {
  font-size: 12px;
  color: var(--color-text-muted);
}

.portal-content {
  padding: 28px 24px;
  flex: 1;
}

.portal-backdrop {
  display: none;
}

/* ---------- Portal widgets ---------- */

.portal-heading h1 {
  font-size: 26px;
  margin-bottom: 6px;
}

.portal-heading p {
  color: var(--color-text-muted);
  margin-bottom: 24px;
}

.stat-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(180px, 1fr));
  gap: 16px;
  margin-bottom: 24px;
}

.stat-card {
  background: var(--color-bg);
  border: 1px solid var(--color-border);
  border-radius: var(--radius-md);
  padding: 20px;
  box-shadow: var(--shadow-sm);
  display: flex;
  flex-direction: column;
  gap: 6px;
}

.stat-label {
  font-size: 12px;
  text-transform: uppercase;
  letter-spacing: 0.05em;
  color: var(--color-text-muted);
  font-weight: 600;
}

.stat-value {
  font-size: 20px;
  font-weight: 700;
}

.stat-value-sm {
  font-size: 15px;
  font-weight: 500;
  word-break: break-all;
}

.stat-value-ok {
  color: var(--color-success);
}

.card {
  background: var(--color-bg);
  border: 1px solid var(--color-border);
  border-radius: var(--radius-md);
  padding: 24px;
  box-shadow: var(--shadow-sm);
}

.card-title {
  font-size: 18px;
  margin-bottom: 8px;
}

.card-body-text {
  color: var(--color-text-muted);
}

.page-loader {
  min-height: 100vh;
  display: grid;
  place-items: center;
  color: var(--color-text-muted);
}

/* ---------- Consultation ---------- */

.link-back {
  background: none;
  border: none;
  color: var(--color-primary);
  font-size: 14px;
  cursor: pointer;
  padding: 0 0 6px;
}

.editor-heading {
  display: flex;
  align-items: flex-start;
  justify-content: space-between;
  gap: 16px;
}

.status-badge {
  display: inline-flex;
  align-items: center;
  gap: 6px;
  padding: 4px 12px;
  border-radius: 999px;
  font-size: 12px;
  font-weight: 700;
  text-transform: uppercase;
  letter-spacing: 0.03em;
  white-space: nowrap;
}

.status-draft {
  background: var(--color-bg-subtle);
  color: var(--color-text-muted);
}
.status-in-progress {
  background: rgb(217 119 6 / 0.15);
  color: var(--color-amber);
}
.status-completed {
  background: rgb(22 163 74 / 0.15);
  color: var(--color-success);
}
.status-cancelled {
  background: rgb(220 38 38 / 0.12);
  color: var(--color-danger);
}

.form-success {
  color: var(--color-success);
  background: rgb(22 163 74 / 0.1);
  border: 1px solid rgb(22 163 74 / 0.25);
  border-radius: var(--radius-sm);
  padding: 10px 12px;
  font-size: 14px;
  margin-bottom: 16px;
  font-weight: 600;
}

.patient-header {
  background: linear-gradient(135deg, var(--color-primary) 0%, var(--color-primary-dark) 100%);
  color: #fff;
  border-radius: var(--radius-lg);
  padding: 20px 24px;
  margin-bottom: 24px;
  box-shadow: var(--shadow-md);
}

.patient-header-top {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 12px;
  margin-bottom: 16px;
  flex-wrap: wrap;
}

.patient-header-name {
  font-size: 20px;
  font-weight: 700;
}

.alert-chip {
  background: rgb(255 255 255 / 0.18);
  padding: 6px 12px;
  border-radius: 999px;
  font-size: 13px;
  font-weight: 600;
}

.patient-header-grid {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(150px, 1fr));
  gap: 14px;
}

.ph-item {
  display: flex;
  flex-direction: column;
}
.ph-item span {
  font-size: 12px;
  opacity: 0.85;
  text-transform: uppercase;
  letter-spacing: 0.04em;
}
.ph-item b {
  font-weight: 600;
  word-break: break-word;
}

.exam-card {
  background: var(--color-bg);
  border: 1px solid var(--color-border);
  border-radius: var(--radius-lg);
  padding: 20px;
  margin-bottom: 20px;
  box-shadow: var(--shadow-sm);
}

.exam-card-head {
  border-bottom: 1px solid var(--color-border);
  padding-bottom: 12px;
  margin-bottom: 16px;
}

.exam-card-title {
  font-size: 17px;
}

.exam-card-desc {
  color: var(--color-text-muted);
  font-size: 13px;
  margin-top: 4px;
}

.field-hint {
  font-size: 12px;
  color: var(--color-text-muted);
}

.choice-strip {
  display: inline-flex;
  gap: 6px;
  flex-wrap: wrap;
}

.choice-pill {
  border: 1px solid var(--color-border);
  background: var(--color-bg-subtle);
  color: var(--color-text-muted);
  border-radius: 999px;
  padding: 7px 16px;
  font-size: 13px;
  font-weight: 600;
  cursor: pointer;
  transition: all 0.12s ease;
}

.choice-pill:hover {
  border-color: var(--color-primary);
  color: var(--color-primary);
}

.choice-pill.is-selected {
  background: var(--color-primary);
  border-color: var(--color-primary);
  color: #fff;
}

.check-pills {
  display: flex;
  flex-wrap: wrap;
  gap: 10px;
}

.check-pill {
  display: inline-flex;
  align-items: center;
  gap: 8px;
  border: 1px solid var(--color-border);
  border-radius: var(--radius-sm);
  padding: 10px 14px;
  background: var(--color-bg);
  cursor: pointer;
  font: inherit;
  font-size: 14px;
  color: var(--color-text);
}

.check-pill.is-selected {
  border-color: var(--color-primary);
  background: var(--color-primary-soft);
  color: var(--color-primary-dark);
  font-weight: 600;
}

.check-box {
  width: 18px;
  height: 18px;
  border: 1.5px solid var(--color-border);
  border-radius: 4px;
  display: grid;
  place-items: center;
  font-size: 12px;
  color: #fff;
  background: var(--color-bg);
}

.check-pill.is-selected .check-box {
  background: var(--color-primary);
  border-color: var(--color-primary);
}

.condition-grid {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(260px, 1fr));
  gap: 16px;
  margin-bottom: 16px;
}

.condition-item {
  display: flex;
  flex-direction: column;
  gap: 8px;
  padding: 12px;
  border: 1px solid var(--color-border);
  border-radius: var(--radius-md);
  background: var(--color-bg-soft);
}

.condition-name {
  font-weight: 600;
  font-size: 14px;
}

.med-row {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(160px, 1fr));
  gap: 12px;
  border: 1px solid var(--color-border);
  border-radius: var(--radius-md);
  padding: 14px;
  margin-bottom: 12px;
  background: var(--color-bg-soft);
  position: relative;
}

.med-row .danger-link {
  grid-column: 1 / -1;
  justify-self: start;
}

.link-danger {
  background: none;
  border: none;
  color: var(--color-danger);
  cursor: pointer;
  font-size: 13px;
  padding: 4px 0;
}

.choice-row {
  display: flex;
  gap: 8px;
  flex-wrap: wrap;
  margin-bottom: 16px;
}

.v-row,
.two-col {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(240px, 1fr));
  gap: 16px;
}

.habits-grid {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(220px, 1fr));
  gap: 16px;
}

.habit-item {
  border: 1px solid var(--color-border);
  border-radius: var(--radius-md);
  padding: 12px;
  background: var(--color-bg-soft);
}

.habit-head {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 12px;
  flex-wrap: wrap;
}

.habit-details {
  margin-top: 12px;
}

.habit-details .field {
  margin-bottom: 10px;
}

.assessment-item {
  border: 1px solid var(--color-border);
  border-radius: var(--radius-md);
  padding: 14px;
  background: var(--color-bg-soft);
  display: flex;
  flex-direction: column;
  gap: 10px;
}

.assessment-head {
  display: flex;
  align-items: flex-start;
  justify-content: space-between;
  gap: 12px;
  flex-wrap: wrap;
}

.assessment-label {
  font-weight: 600;
  font-size: 14px;
}

.assessment-notes,
.textarea-textarea,
.findings-textarea {
  width: 100%;
  font: inherit;
  padding: 10px 12px;
  border: 1px solid var(--color-border);
  border-radius: var(--radius-sm);
  resize: vertical;
}

.exam-g,
.assess-g {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(250px, 1fr));
  gap: 16px;
}

.consult-actions {
  display: flex;
  flex-wrap: wrap;
  gap: 12px;
  align-items: center;
  margin-top: 8px;
  padding: 16px 0;
  border-top: 1px solid var(--color-border);
}

.muted {
  color: var(--color-text-muted);
}

.line-mt {
  margin-top: 12px;
}

.v-row .field {
  margin-bottom: 14px;
}

.search-row {
  display: flex;
  gap: 10px;
  margin-bottom: 16px;
  flex-wrap: wrap;
}

.search-input {
  flex: 1;
  min-width: 200px;
  font: inherit;
  padding: 11px 14px;
  border: 1px solid var(--color-border);
  border-radius: var(--radius-sm);
}

.mt {
  margin-top: 8px;
}

.patient-results {
  display: flex;
  flex-direction: column;
  gap: 8px;
  margin-bottom: 12px;
}

.patient-result-row {
  display: flex;
  flex-direction: column;
  align-items: flex-start;
  gap: 2px;
  text-align: left;
  border: 1px solid var(--color-border);
  background: var(--color-bg);
  border-radius: var(--radius-md);
  padding: 12px 14px;
  cursor: pointer;
  font: inherit;
}

.patient-result-row:hover,
.patient-result-row.is-selected {
  border-color: var(--color-primary);
  box-shadow: var(--shadow-sm);
}

.patient-result-row.is-selected {
  background: var(--color-primary-soft);
}

.patient-result-name {
  font-weight: 600;
}

.patient-result-meta {
  color: var(--color-text-muted);
  font-size: 13px;
}

.new-patient-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(170px, 1fr));
  gap: 10px;
  margin-bottom: 8px;
}

.new-patient-actions {
  display: flex;
  gap: 8px;
}

.patient-summary {
  margin-bottom: 14px;
}

.patient-summary-id {
  font-weight: 700;
  font-size: 17px;
}

.patient-summary-meta {
  color: var(--color-text-muted);
  font-size: 14px;
}

.timeline {
  display: flex;
  flex-direction: column;
  gap: 8px;
}

.timeline-item {
  display: flex;
  gap: 14px;
  align-items: flex-start;
  text-align: left;
  border: 1px solid var(--color-border);
  background: var(--color-bg);
  border-radius: var(--radius-md);
  padding: 12px 14px;
  cursor: pointer;
  font: inherit;
  width: 100%;
}

.timeline-item:hover {
  border-color: var(--color-primary);
}

.timeline-date {
  min-width: 84px;
  font-weight: 600;
  font-size: 13px;
  color: var(--color-primary-dark);
}

.timeline-body {
  display: flex;
  flex-direction: column;
  gap: 4px;
}

.timeline-title {
  font-weight: 600;
}

.timeline-sub {
  display: flex;
  align-items: center;
  gap: 10px;
  color: var(--color-text-muted);
  font-size: 13px;
  flex-wrap: wrap;
}

.state-card {
  background: var(--color-bg);
  border: 1px solid var(--color-border);
  border-radius: var(--radius-lg);
  padding: 40px;
  text-align: center;
  box-shadow: var(--shadow-sm);
}

.link-back {
  background: none;
  border: none;
  color: var(--color-primary);
  font-size: 14px;
  font-weight: 600;
  cursor: pointer;
  padding: 0 0 6px;
}

.complete-text {
  color: var(--color-text-muted);
  font-size: 13px;
}

/* ---------- Responsive ---------- */

@media (max-width: 900px) {
  .hero {
    grid-template-columns: 1fr;
    gap: 32px;
  }

  .portal-sidebar {
    position: fixed;
    left: 0;
    top: 0;
    height: 100vh;
    transform: translateX(-100%);
    transition: transform 0.2s ease;
    z-index: 30;
  }

  .portal-sidebar.is-open {
    transform: translateX(0);
  }

  .sidebar-close {
    display: block;
  }

  .portal-backdrop {
    display: block;
    position: fixed;
    inset: 0;
    background: rgb(15 23 42 / 0.4);
    z-index: 20;
    opacity: 0;
    pointer-events: none;
    transition: opacity 0.2s ease;
  }

  .portal-backdrop.is-open {
    opacity: 1;
    pointer-events: auto;
  }

  .menu-toggle {
    display: block;
  }

  .portal-content {
    padding: 20px 16px;
  }
}

@media (max-width: 640px) {
  .site-main {
    padding: 40px 0;
  }

  .site-nav {
    display: none;
  }

  .header-inner {
    height: 64px;
  }

  .container {
    padding: 0 16px;
  }
}

/* ---------- Digital Tooth Chart ---------- */

.tooth-chart-module {
  display: flex;
  flex-direction: column;
  gap: 8px;
}

.tooth-chart {
  background: var(--color-bg-soft);
  border: 1px solid var(--color-border);
  border-radius: var(--radius-lg);
  padding: 18px 14px;
}

/* Only the chart scrolls horizontally on small screens; the page stays overflow-free. */
.tooth-chart-scroll {
  overflow-x: auto;
  -webkit-overflow-scrolling: touch;
  padding: 4px 2px 12px;
}

.tooth-chart-scroll::-webkit-scrollbar {
  height: 8px;
}

.tooth-chart-scroll::-webkit-scrollbar-thumb {
  background: var(--color-border);
  border-radius: 99px;
}

.tooth-chart-title {
  text-align: center;
  font-size: 12px;
  font-weight: 700;
  letter-spacing: 0.08em;
  text-transform: uppercase;
  color: var(--color-text-muted);
  padding: 4px 0;
}

.tooth-chart-row {
  display: flex;
  align-items: flex-start;
  justify-content: center;
  gap: 14px;
  padding: 6px 0;
}

.tooth-quadrant {
  display: grid;
  grid-template-columns: repeat(8, auto);
  gap: 5px;
}

.tooth {
  position: relative;
  width: 50px;
  height: 54px;
  border-radius: 10px;
  border: 1.5px solid var(--color-border);
  background: var(--color-bg);
  color: var(--color-text);
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  gap: 2px;
  cursor: pointer;
  font-family: inherit;
  transition:
    transform 0.12s ease,
    border-color 0.12s ease,
    box-shadow 0.12s ease;
  padding: 2px;
}

.tooth:hover {
  border-color: var(--color-primary);
  transform: translateY(-1px);
}

.tooth-number {
  font-size: 13px;
  font-weight: 700;
  line-height: 1;
}

.tooth-code {
  font-size: 8.5px;
  font-weight: 800;
  letter-spacing: 0.03em;
  text-transform: uppercase;
  line-height: 1;
  opacity: 0.9;
}

/* Status colours (symbol always shown via code, so accessible beyond colour). */
.tooth-healthy {
  background: var(--color-bg);
}
.tooth-caries {
  background: rgb(217 119 6 / 0.16);
  border-top: 4px solid var(--color-amber);
  color: #92400e;
}
.tooth-filling {
  background: rgb(14 165 233 / 0.16);
  border-top: 4px solid var(--color-accent);
  color: #0369a1;
}
.tooth-rct {
  background: rgb(124 58 237 / 0.14);
  border-top: 4px solid #7c3aed;
  color: #5b21b6;
}
.tooth-crown {
  background: rgb(14 116 144 / 0.16);
  border-top: 4px solid #0e7490;
  color: #155e75;
}
.tooth-bridge {
  background: rgb(79 70 229 / 0.14);
  border-top: 4px solid #4f46e5;
  color: #3730a3;
}
.tooth-implant {
  background: rgb(22 163 74 / 0.14);
  border-top: 4px solid var(--color-success);
  color: #15803d;
}
.tooth-extraction-required {
  background: rgb(220 38 38 / 0.12);
  border-top: 4px solid var(--color-danger);
  color: #b91c1c;
}
.tooth-other {
  background: var(--color-bg-subtle);
  border-top: 4px solid var(--color-text-muted);
  color: #475569;
}

.tooth.is-missing {
  background: repeating-linear-gradient(
    45deg,
    #f1f5f9,
    #f1f5f9 4px,
    #e2e8f0 4px,
    #e2e8f0 8px
  );
  color: #94a3b8;
  border-style: solid;
  border-top: 4px solid var(--color-text-muted);
}

.tooth.is-selected {
  outline: 3px solid var(--color-primary-light);
  outline-offset: 1px;
  border-color: var(--color-primary);
  box-shadow: var(--shadow-md);
  transform: scale(1.06);
  z-index: 1;
}

.tooth.is-disabled {
  cursor: default;
  opacity: 1;
}

.tooth.is-disabled:hover {
  transform: none;
  border-color: var(--color-border);
}

.tooth-legend {
  display: flex;
  flex-wrap: wrap;
  gap: 6px 14px;
  padding: 4px 0 0;
  font-size: 12px;
  color: var(--color-text-muted);
}

.tooth-legend-item {
  display: inline-flex;
  align-items: center;
  gap: 6px;
  white-space: nowrap;
}

.legend-dot {
  width: 11px;
  height: 11px;
  border-radius: 3px;
  border: 1px solid var(--color-border);
  background: var(--color-bg);
  display: inline-block;
}

.dot-caries { background: rgb(217 119 6 / 0.4); border-color: var(--color-amber); }
.dot-missing { background: repeating-linear-gradient(45deg, #cbd5e1, #cbd5e1 2px, #94a3b8 2px, #94a3b8 4px); }
.dot-filling { background: rgb(14 165 233 / 0.4); border-color: var(--color-accent); }
.dot-rct { background: rgb(124 58 237 / 0.4); border-color: #7c3aed; }
.dot-crown { background: rgb(14 116 144 / 0.4); border-color: #0e7490; }
.dot-bridge { background: rgb(79 70 229 / 0.4); border-color: #4f46e5; }
.dot-implant { background: rgb(22 163 74 / 0.4); border-color: var(--color-success); }
.dot-extraction-required { background: rgb(220 38 38 / 0.4); border-color: var(--color-danger); }
.dot-other { background: var(--color-bg-subtle); border-color: var(--color-text-muted); }

/* Tooth detail modal */
.tooth-modal-backdrop {
  position: fixed;
  inset: 0;
  background: rgb(15 23 42 / 0.45);
  z-index: 50;
  display: flex;
  align-items: flex-start;
  justify-content: center;
  padding: 24px 16px;
  overflow-y: auto;
}

.tooth-modal {
  width: min(600px, 100%);
  max-height: 88vh;
  overflow-y: auto;
  background: var(--color-bg);
  border-radius: var(--radius-lg);
  box-shadow: var(--shadow-lg);
  padding: 22px;
}

.tooth-modal-head {
  display: flex;
  align-items: flex-start;
  justify-content: space-between;
  gap: 12px;
  border-bottom: 1px solid var(--color-border);
  padding-bottom: 12px;
  margin-bottom: 16px;
}

.tooth-modal-title {
  font-size: 20px;
  font-weight: 800;
}

.tooth-modal-sub {
  margin-top: 4px;
  display: flex;
  align-items: center;
  gap: 8px;
  flex-wrap: wrap;
}

.tooth-status-chip {
  display: inline-flex;
  align-items: center;
  padding: 3px 10px;
  border-radius: 999px;
  font-size: 12px;
  font-weight: 700;
}
.chip-caries { background: rgb(217 119 6 / 0.15); color: #92400e; }
.chip-filling { background: rgb(14 165 233 / 0.15); color: #0369a1; }
.chip-rct { background: rgb(124 58 237 / 0.15); color: #5b21b6; }
.chip-crown { background: rgb(14 116 144 / 0.15); color: #155e75; }
.chip-bridge { background: rgb(79 70 229 / 0.15); color: #3730a3; }
.chip-implant { background: rgb(22 163 74 / 0.15); color: #15803d; }
.chip-extraction-required { background: rgb(220 38 38 / 0.15); color: #b91c1c; }
.chip-other, .chip-healthy { background: var(--color-bg-subtle); color: var(--color-text-muted); }
.chip-missing { background: rgb(148 163 184 / 0.2); color: #475569; }

.tooth-status-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(160px, 1fr));
  gap: 12px;
  margin-bottom: 18px;
}

.tooth-status-card {
  border: 1px solid var(--color-border);
  border-radius: var(--radius-md);
  padding: 12px;
  background: var(--color-bg-soft);
  display: flex;
  flex-direction: column;
  gap: 4px;
}
.tooth-status-card span {
  font-size: 11px;
  text-transform: uppercase;
  letter-spacing: 0.04em;
  color: var(--color-text-muted);
  font-weight: 700;
}
.tooth-status-card b {
  font-size: 15px;
}
.tooth-status-card em {
  font-style: normal;
  color: var(--color-text-muted);
  font-size: 13px;
}
.tooth-status-card.latest {
  border-color: rgb(22 163 74 / 0.4);
  background: rgb(22 163 74 / 0.06);
}
.tooth-status-card.planned {
  border-color: rgb(217 119 6 / 0.4);
  background: rgb(217 119 6 / 0.06);
}

.tooth-section {
  margin-bottom: 20px;
}
.tooth-section h4 {
  font-size: 15px;
  margin-bottom: 10px;
  border-bottom: 1px solid var(--color-border);
  padding-bottom: 8px;
}

.tooth-timeline {
  display: flex;
  flex-direction: column;
  gap: 8px;
}

.tooth-timeline-item {
  display: flex;
  gap: 12px;
  align-items: flex-start;
  padding: 10px 0;
  border-bottom: 1px dashed var(--color-border);
}
.tooth-timeline-item:last-child {
  border-bottom: none;
}

.ttl-date {
  min-width: 88px;
  font-weight: 700;
  font-size: 12.5px;
  color: var(--color-primary-dark);
  padding-top: 2px;
}

.ttl-body {
  display: flex;
  flex-direction: column;
  gap: 3px;
}

.ttl-title {
  font-weight: 700;
  font-size: 14px;
  display: flex;
  align-items: center;
  gap: 6px;
  flex-wrap: wrap;
}
.ttl-title:first-child {
  color: var(--color-text-muted);
  font-weight: 600;
}
.ttl-arrow {
  color: var(--color-text-muted);
}
.ttl-desc,
.ttl-meta {
  color: var(--color-text-muted);
  font-size: 13px;
}
.ttl-meta-small {
  color: var(--color-text-muted);
  font-size: 12px;
  display: flex;
  gap: 12px;
  flex-wrap: wrap;
}

.tooth-form {
  display: flex;
  flex-direction: column;
  gap: 4px;
}

.tooth-form-grid {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 12px;
}

.tooth-actions {
  display: flex;
  gap: 10px;
  flex-wrap: wrap;
  margin: 8px 0;
}

.page-loader-inline {
  color: var(--color-text-muted);
  padding: 16px 0;
}

.state-card-sm {
  padding: 20px;
}

/* Tooth chart responsive */
@media (max-width: 640px) {
  .tooth {
    width: 46px;
    height: 50px;
  }
  .tooth-number {
    font-size: 12px;
  }
  .tooth-modal {
    padding: 16px;
  }
  .tooth-modal-backdrop {
    padding: 12px;
    align-items: stretch;
  }
  .tooth-form-grid {
    grid-template-columns: 1fr;
  }
.tooth-chart-title,
  .tooth-legend {
    text-align: left;
  }
}

.form-grid {
  display: grid;
  grid-template-columns: repeat(2, 1fr);
  gap: 12px;
}

/* ------------------------- Diagnosis & Treatment Plan ------------------------- */

.diag-list {
  list-style: none;
  margin: 0 0 14px;
  padding: 0;
  display: flex;
  flex-direction: column;
  gap: 8px;
}

.diag-row {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 12px;
  padding: 10px 12px;
  border: 1px solid var(--color-border);
  border-radius: 10px;
  background: var(--color-surface-alt);
}

.diag-main {
  display: flex;
  flex-direction: column;
  gap: 2px;
  min-width: 0;
}

.diag-name {
  font-weight: 600;
  color: var(--color-text);
}

.diagnosis-form {
  display: flex;
  flex-direction: column;
  gap: 12px;
  border: 1px solid var(--color-border);
  border-radius: 12px;
  padding: 14px;
  margin-top: 12px;
  background: var(--color-surface-alt);
}

.plan-list {
  display: flex;
  flex-direction: column;
  gap: 12px;
  margin-bottom: 14px;
}

.plan-card {
  border: 1px solid var(--color-border);
  border-radius: 12px;
  overflow: hidden;
  background: var(--color-surface);
}

.plan-card.is-active {
  border-color: var(--color-primary);
  box-shadow: 0 0 0 1px var(--color-primary);
}

.plan-card-head {
  width: 100%;
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 12px;
  padding: 12px 14px;
  background: transparent;
  border: none;
  cursor: pointer;
  text-align: left;
  flex-wrap: wrap;
}

.plan-card-head:hover {
  background: var(--color-surface-alt);
}

.plan-card-head-static {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 12px;
  padding: 12px 14px;
  flex-wrap: wrap;
}

.plan-head-main {
  display: flex;
  flex-direction: column;
  gap: 2px;
  min-width: 0;
}

.plan-number {
  font-weight: 700;
  color: var(--color-primary);
  font-size: 0.9rem;
}

.plan-name {
  font-weight: 600;
  color: var(--color-text);
}

.plan-head-meta {
  display: flex;
  align-items: center;
  gap: 10px;
  flex-wrap: wrap;
}

.plan-body {
  padding: 0 14px 14px;
  border-top: 1px solid var(--color-border);
  display: flex;
  flex-direction: column;
  gap: 12px;
}

.plan-actions {
  display: flex;
  gap: 8px;
  flex-wrap: wrap;
  padding-top: 12px;
}

.decline-box {
  border: 1px solid var(--color-danger, #dc2626);
  border-radius: 10px;
  padding: 10px;
  display: flex;
  flex-direction: column;
  gap: 10px;
}

.plan-item-list {
  list-style: none;
  margin: 0;
  padding: 0;
  display: flex;
  flex-direction: column;
  gap: 8px;
}

.plan-item {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 10px;
  padding: 9px 11px;
  border: 1px solid var(--color-border);
  border-radius: 9px;
  background: var(--color-surface-alt);
}

.plan-item-main {
  display: flex;
  flex-direction: column;
  gap: 2px;
  min-width: 0;
}

.plan-item-proc {
  font-weight: 600;
  color: var(--color-text);
}

.plan-item-actions {
  display: flex;
  align-items: center;
  gap: 6px;
}

.plan-item-form {
  display: flex;
  flex-direction: column;
  gap: 12px;
  border: 1px dashed var(--color-border-strong, var(--color-border));
  border-radius: 10px;
  padding: 12px;
}

.plan-create-form {
  display: flex;
  flex-direction: column;
  gap: 12px;
  border: 1px solid var(--color-border);
  border-radius: 12px;
  padding: 14px;
  margin-top: 12px;
  background: var(--color-surface-alt);
}

.tooth-clinical-block {
  display: flex;
  flex-direction: column;
  gap: 6px;
  padding: 10px 12px;
  border: 1px solid var(--color-border);
  border-radius: 10px;
  background: var(--color-surface-alt);
  margin-bottom: 10px;
}

.small-select {
  width: auto;
  padding: 6px 10px;
  border: 1px solid var(--color-border);
  border-radius: 8px;
  background: var(--color-surface);
  font-size: 0.85rem;
}

.btn-block {
  width: 100%;
  justify-content: center;
}

.btn-sm {
  padding: 6px 12px;
  font-size: 0.85rem;
}

.btn-danger {
  background: #dc2626;
  color: #fff;
}

.btn-danger:hover {
  background: #b91c1c;
}

.btn-icon {
  border: none;
  background: transparent;
  color: var(--color-text-muted);
  font-size: 1.2rem;
  cursor: pointer;
  line-height: 1;
  width: 28px;
  height: 28px;
  border-radius: 6px;
}

.btn-icon:hover {
  background: var(--color-surface-alt);
  color: #dc2626;
}

.clinical-list-group {
  margin-bottom: 18px;
}

.clinical-list-head {
  font-size: 0.85rem;
  text-transform: uppercase;
  letter-spacing: 0.04em;
  color: var(--color-text-muted);
  margin: 0 0 8px;
}

.form-actions {
  display: flex;
  gap: 10px;
  justify-content: flex-end;
  flex-wrap: wrap;
}

/* Diagnosis & treatment plan responsive */
@media (max-width: 640px) {
  .diag-row,
  .plan-item {
    flex-direction: column;
    align-items: stretch;
  }
  .form-grid {
    grid-template-columns: 1fr;
  }
  .plan-card-head,
  .plan-card-head-static {
    flex-direction: column;
    align-items: flex-start;
  }
}

/* ------------------------------- Prescription ------------------------------- */

.rx-list,
.inv-list {
  display: flex;
  flex-direction: column;
  gap: 12px;
}

.rx-card,
.inv-card {
  border: 1px solid var(--color-border);
  border-radius: 12px;
  padding: 12px 14px;
  background: var(--color-surface);
  display: flex;
  flex-direction: column;
  gap: 8px;
}

.rx-card-head,
.inv-card-head {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 10px;
  flex-wrap: wrap;
}

.rx-head-main,
.inv-head-main {
  display: flex;
  flex-direction: column;
  gap: 2px;
  min-width: 0;
}

.rx-items {
  list-style: none;
  margin: 0;
  padding: 0;
  display: flex;
  flex-direction: column;
  gap: 6px;
}

.rx-item {
  padding: 8px 10px;
  border: 1px solid var(--color-border);
  border-radius: 9px;
  background: var(--color-surface-alt);
}

.rx-item-main {
  display: flex;
  flex-direction: column;
  gap: 2px;
}

.rx-item-name {
  font-weight: 600;
  color: var(--color-text);
}

.rx-item-instructions {
  color: var(--color-primary);
  font-size: 0.85rem;
}

.rx-form,
.inv-form {
  display: flex;
  flex-direction: column;
  gap: 12px;
  border: 1px solid var(--color-border);
  border-radius: 12px;
  padding: 14px;
  margin-top: 12px;
  background: var(--color-surface-alt);
}

.rx-form-items {
  display: flex;
  flex-direction: column;
  gap: 12px;
}

.rx-line {
  display: flex;
  flex-direction: column;
  gap: 10px;
  border: 1px solid var(--color-border);
  border-radius: 10px;
  padding: 12px;
  background: var(--color-surface);
}

.rx-line-head {
  display: flex;
  align-items: center;
  justify-content: space-between;
}

/* ------------------------------- Investigation ------------------------------- */

.inv-name {
  font-weight: 600;
  color: var(--color-text);
}

.inv-result {
  border: 1px solid var(--color-border);
  border-left: 3px solid var(--color-primary);
  border-radius: 9px;
  padding: 9px 11px;
  background: var(--color-surface-alt);
  display: flex;
  flex-direction: column;
  gap: 3px;
}

.inv-result-head {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 8px;
  flex-wrap: wrap;
}

.inv-result-label {
  font-weight: 700;
  color: var(--color-primary);
  font-size: 0.85rem;
  text-transform: uppercase;
  letter-spacing: 0.04em;
}

.inv-result-history {
  border-left-color: var(--color-text-muted);
}

.inv-history {
  border: 1px solid var(--color-border);
  border-radius: 9px;
  padding: 8px 10px;
}

.inv-history summary {
  cursor: pointer;
  font-weight: 600;
  color: var(--color-text);
}

.inv-result-box {
  display: flex;
  flex-direction: column;
  gap: 10px;
  border: 1px dashed var(--color-border-strong, var(--color-border));
  border-radius: 10px;
  padding: 12px;
}

/* ------------------------------- Print view ------------------------------- */

.print-page {
  max-width: 820px;
  margin: 0 auto;
  padding: 24px;
  font-family: var(--font-body, inherit);
}

.print-toolbar {
  display: flex;
  justify-content: flex-end;
  margin-bottom: 12px;
}

.rx-print-sheet {
  background: #fff;
  border: 1px solid var(--color-border);
  border-radius: 12px;
  padding: 28px;
  color: #111;
}

.rx-print-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding-bottom: 12px;
  border-bottom: 2px solid var(--color-primary);
  margin-bottom: 16px;
}

.rx-print-brand {
  font-size: 1.4rem;
  font-weight: 800;
  color: var(--color-primary);
}

.rx-print-number {
  font-weight: 700;
  color: var(--color-text-muted);
}

.rx-print-meta {
  display: flex;
  justify-content: space-between;
  gap: 24px;
  margin-bottom: 20px;
  flex-wrap: wrap;
}

.rx-print-meta-col {
  display: flex;
  flex-direction: column;
  gap: 2px;
}

.rx-print-label {
  font-size: 0.75rem;
  text-transform: uppercase;
  letter-spacing: 0.05em;
  color: var(--color-text-muted);
}

.rx-print-table {
  width: 100%;
  border-collapse: collapse;
  margin-bottom: 16px;
}

.rx-print-table th,
.rx-print-table td {
  text-align: left;
  padding: 8px 10px;
  border: 1px solid #ddd;
  vertical-align: top;
}

.rx-print-table th {
  background: #f3f6f8;
  font-size: 0.8rem;
  text-transform: uppercase;
  letter-spacing: 0.03em;
}

.rx-print-instructions {
  color: var(--color-primary);
  font-size: 0.85rem;
  margin-top: 2px;
}

.rx-print-notes {
  margin-bottom: 16px;
}

.rx-print-footer {
  margin-top: 20px;
  padding-top: 12px;
  border-top: 1px solid #ddd;
  color: var(--color-text-muted);
  font-size: 0.85rem;
}

@media print {
  .no-print {
    display: none !important;
  }
  .print-page {
    padding: 0;
  }
  .rx-print-sheet {
    border: none;
    box-shadow: none;
  }
}

/* Prescription & investigation responsive */
@media (max-width: 640px) {
  .rx-print-meta {
    flex-direction: column;
    gap: 12px;
  }
  .rx-print-sheet {
    padding: 16px;
    overflow-x: auto;
  }
}

/* ------------------------------- Billing & Payments ------------------------------- */

.billing-toolbar {
  display: flex;
  flex-wrap: wrap;
  gap: 10px;
  margin: 4px 0 14px;
}

.billing-head-right {
  display: flex;
  align-items: center;
  gap: 8px;
  flex-wrap: wrap;
}

.billing-table {
  width: 100%;
  border-collapse: collapse;
  margin: 6px 0 4px;
  font-size: 0.9rem;
}

.billing-table th,
.billing-table td {
  text-align: left;
  padding: 8px 8px;
  border-bottom: 1px solid var(--color-border);
}

.billing-table th {
  font-size: 0.72rem;
  text-transform: uppercase;
  letter-spacing: 0.04em;
  color: var(--color-text-muted);
  background: var(--color-surface-alt);
}

.billing-table th:last-child,
.billing-table td:last-child {
  width: 34px;
  text-align: center;
}

.bill-tag {
  margin-left: 6px;
  font-size: 0.7rem;
  text-transform: uppercase;
  letter-spacing: 0.03em;
  color: var(--color-primary);
}

.billing-totals {
  display: flex;
  flex-direction: column;
  gap: 4px;
  padding: 8px 0;
  margin-left: auto;
  width: 100%;
  max-width: 360px;
}

.billing-totals > div {
  display: flex;
  justify-content: space-between;
  gap: 12px;
  font-size: 0.9rem;
}

.billing-totals .billing-grand {
  border-top: 2px solid var(--color-border);
  margin-top: 4px;
  padding-top: 8px;
  font-weight: 800;
  font-size: 1.05rem;
}

.billing-balance-owed {
  color: var(--color-danger);
  font-weight: 700;
}

.billing-payments {
  display: flex;
  flex-direction: column;
  gap: 6px;
  padding: 8px 10px;
  background: var(--color-surface-alt);
  border-radius: 10px;
}

.billing-subhead {
  font-size: 0.72rem;
  font-weight: 700;
  text-transform: uppercase;
  letter-spacing: 0.05em;
  color: var(--color-text-muted);
  margin: 0;
}

.billing-payment-row {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 10px;
  font-size: 0.9rem;
  flex-wrap: wrap;
}

.billing-refund-amt {
  color: var(--color-danger);
  font-weight: 700;
}

.billing-actions {
  display: flex;
  flex-wrap: wrap;
  gap: 8px;
  margin-top: 4px;
}

.billing-line-head {
  display: flex;
  flex-wrap: wrap;
  gap: 8px;
}

.billing-summary-chips {
  display: flex;
  flex-wrap: wrap;
  gap: 8px;
  margin-bottom: 12px;
}

.chip-danger {
  background: rgb(220 38 38 / 0.12);
  color: var(--color-danger);
}

.small-muted {
  font-size: 0.82rem;
}

.status-paid {
  background: rgb(22 163 74 / 0.15);
  color: var(--color-success);
}

.status-unpaid {
  background: rgb(220 38 38 / 0.12);
  color: var(--color-danger);
}

.status-partially-paid {
  background: rgb(217 119 6 / 0.15);
  color: var(--color-amber);
}

.status-refunded {
  background: rgb(147 51 234 / 0.12);
  color: #9333ea;
}

.status-finalized {
  background: rgb(22 163 74 / 0.15);
  color: var(--color-success);
}

@media (max-width: 640px) {
  .billing-table {
    font-size: 0.82rem;
  }
  .billing-table th:nth-child(3),
  .billing-table td:nth-child(3) {
    display: none;
  }
  .billing-totals {
    max-width: 100%;
  }
  .billing-actions {
    flex-direction: column;
    align-items: stretch;
  }
  .billing-actions .btn {
    width: 100%;
  }
}

/* ------------------------- Pharmacy / Inventory ------------------------- */

.stat-grid {
  display: grid;
  grid-template-columns: repeat(6, minmax(0, 1fr));
  gap: 16px;
  margin-bottom: 20px;
}

.stat-card {
  background: var(--color-surface);
  border: 1px solid var(--color-border);
  border-radius: 16px;
  padding: 18px;
  box-shadow: 0 1px 3px var(--shadow);
  display: flex;
  flex-direction: column;
  gap: 6px;
}

.stat-value {
  font-size: 30px;
  font-weight: 800;
  color: var(--color-primary);
  line-height: 1.1;
}

.stat-label {
  font-size: 13px;
  color: var(--color-muted);
  font-weight: 600;
}

.dispense-line {
  border-top: 1px solid var(--color-border);
  padding-top: 10px;
  margin-top: 10px;
}

.dispense-controls {
  display: flex;
  gap: 10px;
  margin-top: 8px;
  flex-wrap: wrap;
}

.dispense-controls select {
  min-width: 260px;
  flex: 1;
}

.dispense-controls input {
  width: 90px;
}

.inventory-list {
  display: flex;
  flex-direction: column;
  gap: 10px;
}

.inventory-row {
  display: flex;
  align-items: center;
  gap: 14px;
  padding: 14px 16px;
  background: var(--color-surface);
  border: 1px solid var(--color-border);
  border-radius: 14px;
  box-shadow: 0 1px 2px var(--shadow);
}

.inventory-main,
.inventory-stock,
.inventory-price {
  display: flex;
  flex-direction: column;
  gap: 3px;
}

.inventory-main {
  flex: 1;
  min-width: 0;
}

.inv-qty {
  font-weight: 700;
  color: var(--color-text);
  white-space: nowrap;
}

.inventory-actions {
  display: flex;
  align-items: center;
  gap: 8px;
  flex-wrap: wrap;
  justify-content: flex-end;
}

.inventory-price {
  font-weight: 700;
  text-align: right;
  white-space: nowrap;
}

.filter-select {
  max-width: 240px;
}

@media (max-width: 1080px) {
  .stat-grid {
    grid-template-columns: repeat(3, minmax(0, 1fr));
  }
}

@media (max-width: 640px) {
  .stat-grid {
    grid-template-columns: repeat(2, minmax(0, 1fr));
  }
  .inventory-row {
    flex-direction: column;
    align-items: flex-start;
  }
  .inventory-actions {
    justify-content: flex-start;
    width: 100%;
  }
  .inventory-price {
    text-align: left;
  }
  .dispense-controls select {
    min-width: 0;
    width: 100%;
  }
}

/* ==========================================================================
   Public website (Task 15)
   ========================================================================== */

/* ---------- Header enhancements ---------- */

.header-actions {
  display: flex;
  align-items: center;
  gap: 12px;
}

.menu-toggle-btn {
  display: none;
  align-items: center;
  justify-content: center;
  width: 44px;
  height: 44px;
  border: 1px solid var(--color-border);
  border-radius: var(--radius-md);
  background: var(--color-bg);
  color: var(--color-text);
  cursor: pointer;
}

.menu-toggle-btn:focus-visible {
  outline: 3px solid var(--color-primary-light);
  outline-offset: 2px;
}

.site-nav-link {
  display: inline-flex;
  align-items: center;
  padding: 8px 14px;
  border-radius: 999px;
  color: var(--color-text-muted);
  font-size: 15px;
  font-weight: 500;
  transition: background 0.15s ease, color 0.15s ease;
}

.site-nav-link:hover {
  color: var(--color-primary);
  background: var(--color-bg-subtle);
}

.site-nav-link.active {
  color: var(--color-primary-dark);
  background: var(--color-primary-soft);
}

.mobile-nav {
  display: none;
  position: fixed;
  inset: var(--header-height) 0 0 0;
  background: var(--color-bg);
  flex-direction: column;
  padding: 24px 20px calc(24px + env(safe-area-inset-bottom));
  gap: 8px;
  z-index: 25;
  overflow-y: auto;
}

.mobile-nav.is-open {
  display: flex;
}

.mobile-nav-links {
  display: flex;
  flex-direction: column;
  gap: 4px;
  margin-bottom: 16px;
}

.mobile-nav-links a {
  padding: 12px 14px;
  border-radius: var(--radius-md);
  font-size: 17px;
  font-weight: 600;
  color: var(--color-text);
}

.mobile-nav-links a:hover,
.mobile-nav-links a.active {
  background: var(--color-primary-soft);
  color: var(--color-primary-dark);
}

.mobile-nav-backdrop {
  position: fixed;
  inset: 0;
  background: transparent;
  border: 0;
  cursor: default;
  z-index: -1;
}

/* ---------- Shared public blocks ---------- */

.pub-section {
  padding: 72px 0;
}

.pub-section-compact {
  padding: 48px 0;
}

.pub-alt-section {
  background: var(--color-bg-soft);
  padding: 72px 0;
}

.section-head {
  max-width: 720px;
  margin: 0 auto 48px;
  text-align: center;
}

.section-head-left {
  margin: 0 0 48px;
  text-align: left;
}

.section-title {
  font-size: clamp(28px, 4vw, 40px);
  line-height: 1.15;
  letter-spacing: -0.02em;
  margin-bottom: 14px;
}

.section-lead {
  font-size: 17px;
  color: var(--color-text-muted);
  line-height: 1.7;
}

.section-cta {
  text-align: center;
  margin-top: 40px;
}

.text-primary {
  color: var(--color-primary);
}

.text-link {
  display: inline-flex;
  align-items: center;
  gap: 6px;
  color: var(--color-primary);
  font-weight: 600;
  font-size: 15px;
}

.text-link:hover {
  text-decoration: none;
  color: var(--color-primary-dark);
}

.card-grid {
  display: grid;
  gap: 24px;
}

.card-grid-2 {
  grid-template-columns: repeat(2, minmax(0, 1fr));
}

.card-grid-3 {
  grid-template-columns: repeat(3, minmax(0, 1fr));
}

.card-grid-4 {
  grid-template-columns: repeat(4, minmax(0, 1fr));
}

.pub-card {
  background: var(--color-bg);
  border: 1px solid var(--color-border);
  border-radius: var(--radius-lg);
  padding: 28px;
  box-shadow: var(--shadow-sm);
  transition: box-shadow 0.2s ease, transform 0.2s ease, border-color 0.2s ease;
}

.pub-card:hover {
  box-shadow: var(--shadow-md);
  border-color: var(--color-primary-light);
  transform: translateY(-3px);
}

.pub-icon-badge {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  width: 56px;
  height: 56px;
  border-radius: var(--radius-md);
  background: var(--color-primary-soft);
  color: var(--color-primary);
  margin-bottom: 18px;
}

.pub-icon-badge-lg {
  width: 72px;
  height: 72px;
  border-radius: var(--radius-lg);
}

.muted {
  color: var(--color-text-muted);
}

/* ---------- Hero ---------- */

.pub-hero {
  background:
    radial-gradient(1200px 480px at 85% -10%, var(--color-primary-soft) 0%, transparent 55%),
    linear-gradient(180deg, var(--color-bg-soft) 0%, var(--color-bg) 100%);
  padding: 72px 0;
}

.pub-hero-inner {
  display: grid;
  grid-template-columns: 1.15fr 0.85fr;
  gap: 56px;
  align-items: center;
}

.pub-hero-left h1 {
  font-size: clamp(34px, 5vw, 56px);
  line-height: 1.08;
  letter-spacing: -0.025em;
  margin-bottom: 22px;
}

.pub-hero-left > p {
  font-size: 18px;
  color: var(--color-text-muted);
  max-width: 560px;
  margin-bottom: 32px;
  line-height: 1.7;
}

.hero-trust {
  display: flex;
  flex-wrap: wrap;
  gap: 20px;
  margin-top: 28px;
}

.hero-trust span {
  display: inline-flex;
  align-items: center;
  gap: 8px;
  font-size: 14px;
  font-weight: 600;
  color: var(--color-text-muted);
}

.hero-illustration {
  position: relative;
  display: grid;
  place-items: center;
  width: 100%;
  aspect-ratio: 4 / 3;
  background: linear-gradient(150deg, #e6fffa, #d8f5ff);
  border-radius: var(--radius-xl);
  border: 1px solid #c9ece6;
  overflow: hidden;
}

.hero-tooth {
  color: var(--color-primary);
  opacity: 0.95;
}

.hero-spark {
  position: absolute;
  color: #0ea5e9;
  animation: pubFloat 5s ease-in-out infinite;
}

.hero-spark-one {
  top: 16%;
  right: 14%;
}

.hero-spark-two {
  bottom: 14%;
  left: 12%;
  animation-delay: 1.6s;
}

@keyframes pubFloat {
  0%,
  100% {
    transform: translateY(0) rotate(0deg);
  }
  50% {
    transform: translateY(-10px) rotate(8deg);
  }
}

/* ---------- Trust / stats ---------- */

.pub-trust {
  border-bottom: 1px solid var(--color-border);
  background: var(--color-bg);
}

.stats-bar {
  display: grid;
  grid-template-columns: repeat(4, minmax(0, 1fr));
  padding: 40px 0;
  margin: 0;
}

.stat-item {
  text-align: center;
  border-left: 1px solid var(--color-border);
  padding: 8px 16px;
}

.stat-item:first-child {
  border-left: 0;
}

.stat-item dd {
  font-size: clamp(26px, 3.4vw, 38px);
  font-weight: 800;
  color: var(--color-primary);
  margin: 0 0 4px;
  letter-spacing: -0.02em;
}

.stat-item dt {
  font-size: 13px;
  color: var(--color-text-muted);
  text-transform: uppercase;
  letter-spacing: 0.04em;
}

/* ---------- Treatment cards ---------- */

.treat-card {
  display: flex;
  flex-direction: column;
}

.treat-card h3 {
  font-size: 20px;
  margin-bottom: 8px;
}

.treat-card > p {
  color: var(--color-text-muted);
  font-size: 15px;
  line-height: 1.65;
  margin-bottom: 16px;
}

.treat-points {
  list-style: none;
  margin: 0 0 20px;
  padding: 0;
  display: flex;
  flex-direction: column;
  gap: 8px;
}

.treat-points li {
  display: flex;
  align-items: flex-start;
  gap: 8px;
  font-size: 14px;
  color: var(--color-text-muted);
}

.treat-points li svg {
  flex-shrink: 0;
  color: var(--color-success);
  margin-top: 3px;
}

.treat-card-foot {
  margin-top: auto;
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 12px;
  border-top: 1px solid var(--color-border);
  padding-top: 16px;
}

.price-tag {
  font-size: 14px;
  color: var(--color-text-muted);
}

.price-tag b {
  color: var(--color-text);
  font-size: 17px;
}

/* ---------- Why choose us ---------- */

.why-card h3 {
  font-size: 18px;
  margin-bottom: 8px;
}

.why-card p {
  color: var(--color-text-muted);
  font-size: 15px;
  line-height: 1.65;
}

/* ---------- Doctors ---------- */

.doctor-card {
  text-align: center;
}

.doctor-avatar {
  width: 88px;
  height: 88px;
  margin: 0 auto 16px;
  border-radius: 50%;
  display: grid;
  place-items: center;
  background: linear-gradient(135deg, var(--color-primary), var(--color-primary-light));
  color: #fff;
  font-size: 30px;
  font-weight: 800;
}

.doctor-card h3 {
  font-size: 19px;
  margin-bottom: 4px;
}

.doctor-spec {
  color: var(--color-text-muted);
  font-size: 14px;
  margin-bottom: 16px;
}

/* ---------- Reviews ---------- */

.review-card {
  display: flex;
  flex-direction: column;
}

.review-stars {
  display: flex;
  gap: 2px;
  margin-bottom: 12px;
}

.star-full {
  color: #f59e0b;
}

.star-muted {
  color: var(--color-border-strong, #cbd5e1);
}

.review-card blockquote {
  margin: 0 0 18px;
  font-size: 15px;
  line-height: 1.7;
  color: var(--color-text);
  flex: 1;
}

.review-card figcaption {
  display: flex;
  flex-direction: column;
  border-top: 1px solid var(--color-border);
  padding-top: 14px;
}

.review-card figcaption strong {
  font-size: 15px;
}

.review-card figcaption span {
  font-size: 13px;
  color: var(--color-text-muted);
}

/* ---------- Page hero (inner pages) ---------- */

.page-hero {
  background:
    radial-gradient(900px 320px at 15% -20%, var(--color-primary-soft) 0%, transparent 60%),
    linear-gradient(180deg, var(--color-bg-soft) 0%, var(--color-bg) 100%);
  padding: 64px 0 8px;
  border-bottom: 1px solid var(--color-border);
}

.page-hero-inner {
  text-align: center;
}

.detail-hero .pub-icon-badge {
  margin-bottom: 20px;
}

.detail-meta {
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 20px;
  margin-top: 8px;
}

/* ---------- Facilities ---------- */

.facilities-grid {
  display: grid;
  grid-template-columns: repeat(2, minmax(0, 1fr));
  gap: 20px;
  max-width: 900px;
  margin: 0 auto;
}

.facility-pill {
  display: flex;
  align-items: flex-start;
  gap: 16px;
  padding: 24px;
  border: 1px solid var(--color-border);
  border-radius: var(--radius-lg);
  background: var(--color-bg);
}

.facility-pill .pub-icon-badge {
  margin-bottom: 0;
  flex-shrink: 0;
  width: 48px;
  height: 48px;
}

.facility-pill strong {
  font-size: 16px;
}

.facility-pill p {
  color: var(--color-text-muted);
  font-size: 14px;
  margin-top: 4px;
  line-height: 1.6;
}

.facility-card h3 {
  font-size: 18px;
  margin-bottom: 8px;
}

.facility-card p {
  color: var(--color-text-muted);
  font-size: 15px;
  line-height: 1.6;
}

/* ---------- FAQ ---------- */

.faq-block .section-head {
  margin-bottom: 36px;
}

.faq-list {
  max-width: 760px;
  margin: 0 auto;
  display: flex;
  flex-direction: column;
  gap: 12px;
}

.faq-item {
  border: 1px solid var(--color-border);
  border-radius: var(--radius-md);
  background: var(--color-bg);
  overflow: hidden;
}

.faq-question {
  width: 100%;
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 16px;
  padding: 18px 20px;
  background: none;
  border: 0;
  font: inherit;
  font-weight: 600;
  font-size: 15px;
  color: var(--color-text);
  text-align: left;
  cursor: pointer;
}

.faq-question:focus-visible {
  outline: 3px solid var(--color-primary-light);
  outline-offset: -3px;
}

.faq-question svg {
  transition: transform 0.2s ease;
  color: var(--color-text-muted);
  flex-shrink: 0;
}

.faq-item.open .faq-question svg {
  transform: rotate(180deg);
}

.faq-answer {
  padding: 0 20px 20px;
  color: var(--color-text-muted);
  font-size: 15px;
  line-height: 1.7;
}

/* ---------- CTA banner ---------- */

.cta-banner {
  background: linear-gradient(120deg, var(--color-primary) 0%, #0c9488 55%, var(--color-accent) 120%);
  color: #fff;
  padding: 64px 0;
}

.cta-banner-inner {
  max-width: 1000px;
  margin: 0 auto;
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 32px;
  padding: 0 24px;
}

.cta-banner h2 {
  font-size: clamp(24px, 3.5vw, 34px);
  margin-bottom: 8px;
}

.cta-banner p {
  font-size: 16px;
  opacity: 0.92;
}

.cta-actions {
  display: flex;
  flex-wrap: wrap;
  gap: 12px;
  flex-shrink: 0;
}

.btn-white {
  background: #fff;
  color: var(--color-primary-dark);
}

.btn-white:hover {
  background: var(--color-primary-soft);
  text-decoration: none;
}

.btn-white-outline {
  border: 1.5px solid rgb(255 255 255 / 0.7);
  color: #fff;
  background: transparent;
}

.btn-white-outline:hover {
  background: rgb(255 255 255 / 0.12);
  text-decoration: none;
}

.btn-lg {
  padding: 14px 28px;
  font-size: 16px;
}

/* ---------- About page ---------- */

.about-grid {
  display: grid;
  grid-template-columns: 1.4fr 0.6fr;
  gap: 48px;
  align-items: start;
}

.about-grid.reverse {
  grid-template-columns: 0.7fr 1.3fr;
  align-items: center;
}

.about-text h2 {
  font-size: clamp(24px, 3vw, 32px);
  margin-bottom: 14px;
}

.about-text p {
  color: var(--color-text-muted);
  font-size: 16px;
  line-height: 1.75;
  margin-bottom: 16px;
}

.about-text h2:not(:first-child) {
  margin-top: 32px;
}

.about-list {
  list-style: none;
  margin: 0;
  padding: 0;
  display: flex;
  flex-direction: column;
  gap: 18px;
}

.about-list li {
  display: flex;
  align-items: flex-start;
  gap: 12px;
}

.about-list li > svg {
  color: var(--color-success);
  margin-top: 4px;
  flex-shrink: 0;
}

.about-list strong {
  font-size: 16px;
}

.about-list p {
  margin: 2px 0 0;
  font-size: 14.5px;
}

.about-highlight {
  position: sticky;
  top: calc(var(--header-height) + 24px);
  background: linear-gradient(150deg, #e6fffa, #eef6ff);
  border: 1px solid #c9ece6;
  border-radius: var(--radius-xl);
  padding: 24px;
}

.about-highlight .stats-bar {
  grid-template-columns: repeat(2, minmax(0, 1fr));
  padding: 0;
  gap: 24px;
}

.about-highlight .stat-item {
  border-left: 1px solid rgb(15 118 110 / 0.2);
}

.about-highlight .stat-item:nth-child(odd) {
  border-left: 0;
}

/* ---------- Booking ---------- */

.booking-grid {
  display: grid;
  grid-template-columns: 0.85fr 1.15fr;
  gap: 48px;
  align-items: start;
  padding-bottom: 88px;
}

.booking-story h2 {
  font-size: 26px;
  margin-bottom: 24px;
}

.booking-card {
  background: var(--color-bg);
  border: 1px solid var(--color-border);
  border-radius: var(--radius-xl);
  box-shadow: var(--shadow-md);
  padding: 32px;
  display: flex;
  flex-direction: column;
  gap: 16px;
}

.field {
  display: flex;
  flex-direction: column;
  gap: 6px;
  margin: 0;
}

.field input,
.field select,
.field textarea {
  width: 100%;
  padding: 11px 14px;
  border: 1px solid var(--color-border);
  border-radius: var(--radius-sm);
  font: inherit;
  font-size: 15px;
  color: var(--color-text);
  background: var(--color-bg);
  transition: border-color 0.15s ease, box-shadow 0.15s ease;
}

.field input:focus,
.field select:focus,
.field textarea:focus {
  outline: none;
  border-color: var(--color-primary-light);
  box-shadow: 0 0 0 3px var(--color-primary-soft);
}

.field-error {
  color: var(--color-danger);
  font-size: 13px;
}

.form-grid.two {
  display: grid;
  grid-template-columns: repeat(2, minmax(0, 1fr));
  gap: 16px;
}

.field-label {
  font-size: 14px;
  font-weight: 600;
  color: var(--color-text);
}

.form-note {
  font-size: 13px;
  color: var(--color-text-muted);
  text-align: center;
}

.form-actions.center {
  justify-content: center;
}

.steps-list {
  list-style: none;
  margin: 0 0 28px;
  padding: 0;
  display: flex;
  flex-direction: column;
  gap: 22px;
}

.steps-list li {
  display: flex;
  align-items: flex-start;
  gap: 14px;
}

.step-num {
  flex-shrink: 0;
  width: 34px;
  height: 34px;
  display: grid;
  place-items: center;
  border-radius: 50%;
  background: var(--color-primary);
  color: #fff;
  font-weight: 700;
  font-size: 15px;
}

.steps-list strong {
  font-size: 16px;
}

.steps-list p {
  color: var(--color-text-muted);
  font-size: 14.5px;
  margin-top: 3px;
  line-height: 1.6;
}

.emergency-note {
  display: flex;
  align-items: flex-start;
  gap: 12px;
  background: #fef3c7;
  border: 1px solid #fde68a;
  border-radius: var(--radius-md);
  padding: 18px;
  margin-top: 8px;
  font-size: 14px;
}

.emergency-note > svg {
  color: var(--color-amber);
  margin-top: 2px;
  flex-shrink: 0;
}

.emergency-note p {
  margin: 2px 0 6px;
  color: var(--color-text-muted);
}

/* ---------- Success ---------- */

.success-section {
  padding-top: 40px;
  padding-bottom: 88px;
}

.success-card {
  max-width: 620px;
  margin: 0 auto;
  background: var(--color-bg);
  border: 1px solid var(--color-border);
  border-radius: var(--radius-xl);
  box-shadow: var(--shadow-md);
  padding: 40px 32px;
  text-align: center;
}

.success-icon {
  width: 72px;
  height: 72px;
  margin: 0 auto 20px;
  display: grid;
  place-items: center;
  border-radius: 50%;
  background: var(--color-primary-soft);
  color: var(--color-primary);
}

.success-card .section-head {
  margin-bottom: 28px;
}

.success-details {
  display: grid;
  grid-template-columns: repeat(2, minmax(0, 1fr));
  gap: 16px;
  text-align: left;
  margin: 0 0 28px;
}

.success-details div {
  background: var(--color-bg-soft);
  border-radius: var(--radius-sm);
  padding: 12px 14px;
}

.success-details dt {
  font-size: 12px;
  text-transform: uppercase;
  letter-spacing: 0.04em;
  color: var(--color-text-muted);
  margin-bottom: 3px;
}

.success-details dd {
  margin: 0;
  font-size: 15px;
  font-weight: 600;
}

/* ---------- Contact ---------- */

.contact-grid {
  display: grid;
  grid-template-columns: 0.9fr 1.1fr;
  gap: 40px;
  align-items: start;
  padding-bottom: 88px;
}

.contact-info {
  display: grid;
  grid-template-columns: repeat(2, minmax(0, 1fr));
  gap: 16px;
}

.contact-card {
  border: 1px solid var(--color-border);
  border-radius: var(--radius-lg);
  padding: 22px;
  background: var(--color-bg);
}

.contact-card .pub-icon-badge {
  width: 48px;
  height: 48px;
  margin-bottom: 14px;
}

.contact-card h3 {
  font-size: 16px;
  margin-bottom: 6px;
}

.contact-card p {
  font-size: 14px;
  color: var(--color-text-muted);
  line-height: 1.6;
  margin-bottom: 14px;
}

.contact-map {
  display: flex;
  flex-direction: column;
  gap: 20px;
}

.map-frame {
  border-radius: var(--radius-lg);
  overflow: hidden;
  border: 1px solid var(--color-border);
  box-shadow: var(--shadow-sm);
}

.map-frame iframe {
  display: block;
  width: 100%;
  height: 340px;
  border: 0;
}

.hours-card {
  border: 1px solid var(--color-border);
  border-radius: var(--radius-lg);
  padding: 22px;
  background: var(--color-bg);
}

.hours-card h3 {
  display: flex;
  align-items: center;
  gap: 8px;
  font-size: 17px;
  margin-bottom: 14px;
}

.footer-hours {
  list-style: none;
  margin: 0 0 10px;
  padding: 0;
  display: flex;
  flex-direction: column;
  gap: 10px;
}

.footer-hours li {
  display: flex;
  align-items: flex-start;
  gap: 10px;
  font-size: 14px;
  color: var(--color-text-muted);
}

.footer-hours li svg {
  color: var(--color-primary);
  margin-top: 3px;
  flex-shrink: 0;
}

.footer-hours strong {
  color: var(--color-text);
}

/* ---------- Detail page ---------- */

.detail-layout {
  display: grid;
  grid-template-columns: 1.35fr 0.65fr;
  gap: 48px;
  align-items: start;
}

.detail-main h2 {
  font-size: 26px;
  margin-bottom: 14px;
}

.detail-main h3 {
  font-size: 20px;
  margin: 32px 0 14px;
}

.detail-main > p {
  color: var(--color-text-muted);
  font-size: 16px;
  line-height: 1.75;
}

.detail-list {
  margin-bottom: 8px;
}

.detail-side {
  display: flex;
  flex-direction: column;
  gap: 16px;
  position: sticky;
  top: calc(var(--header-height) + 24px);
}

.side-card {
  border: 1px solid var(--color-border);
  border-radius: var(--radius-lg);
  padding: 22px;
  background: var(--color-bg);
}

.side-card h3 {
  font-size: 16px;
  margin-bottom: 12px;
}

.side-list {
  list-style: none;
  margin: 0;
  padding: 0;
  display: flex;
  flex-direction: column;
  gap: 10px;
}

.side-list li {
  display: flex;
  align-items: flex-start;
  gap: 10px;
  font-size: 14px;
  color: var(--color-text-muted);
}

.side-list li svg {
  color: var(--color-primary);
  margin-top: 3px;
  flex-shrink: 0;
}

.detail-next {
  max-width: 420px;
  margin: 0 auto;
}

/* ---------- Pricing table ---------- */

.scoring-treatments {
  border-top: 1px solid var(--color-border);
  border-bottom: 1px solid var(--color-border);
}

.price-table {
  max-width: 720px;
  margin: 0 auto;
  border-radius: var(--radius-lg);
  overflow: hidden;
  border: 1px solid var(--color-border);
  background: var(--color-bg);
}

.price-row {
  display: flex;
  align-items: center;
  gap: 16px;
  padding: 16px 24px;
  border-bottom: 1px solid var(--color-border);
  color: var(--color-text);
  transition: background 0.15s ease;
}

.price-row:last-child {
  border-bottom: 0;
}

.price-row:hover {
  background: var(--color-bg-soft);
  text-decoration: none;
}

.price-name {
  flex: 1;
  font-weight: 600;
}

.price-value {
  color: var(--color-primary-dark);
  font-weight: 700;
  font-size: 15px;
}

.price-row svg {
  color: var(--color-text-muted);
}

/* ---------- Footer variations ---------- */

.footer-lg {
  border-top: 1px solid var(--color-border);
  background: #062b28;
  color: #cbd5e1;
  padding: 56px 0 0;
  margin-top: 0;
}

.footer-grid {
  display: grid;
  grid-template-columns: 1.4fr 0.8fr 0.8fr 1.2fr;
  gap: 40px;
  padding-bottom: 40px;
}

.footer-lg .brand {
  color: #fff;
  margin-bottom: 14px;
}

.footer-lg .brand-name span {
  color: var(--color-primary-light);
}

.footer-blurb {
  color: #94a3b8;
  font-size: 14.5px;
  line-height: 1.7;
  margin: 4px 0 20px;
  max-width: 320px;
}

.footer-contact {
  display: flex;
  flex-direction: column;
  gap: 10px;
}

.footer-contact a {
  display: inline-flex;
  align-items: center;
  gap: 8px;
  color: #cbd5e1;
  font-size: 14px;
}

.footer-contact a:hover {
  color: #fff;
}

.footer-title {
  font-size: 15px;
  color: #fff;
  margin-bottom: 16px;
}

.footer-links {
  list-style: none;
  margin: 0;
  padding: 0;
  display: flex;
  flex-direction: column;
  gap: 10px;
}

.footer-links a {
  color: #94a3b8;
  font-size: 14.5px;
}

.footer-links a:hover {
  color: #fff;
}

.footer-address {
  display: flex;
  align-items: flex-start;
  gap: 8px;
  color: #94a3b8;
  font-size: 14px;
  line-height: 1.6;
  margin-bottom: 16px;
}

.footer-address svg {
  color: var(--color-primary-light);
  margin-top: 3px;
  flex-shrink: 0;
}

.footer-lg .footer-hours li {
  color: #94a3b8;
}

.footer-lg .footer-hours svg {
  color: var(--color-primary-light);
}

.footer-lg .footer-hours strong {
  color: #cbd5e1;
}

.footer-bottom {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 16px;
  flex-wrap: wrap;
  border-top: 1px solid rgb(255 255 255 / 0.1);
  padding-top: 22px;
  padding-bottom: 22px;
  font-size: 13.5px;
  color: #94a3b8;
}

.footer-bottom a {
  color: #cbd5e1;
}

/* ---------- Public responsive ---------- */

@media (max-width: 1024px) {
  .pub-hero-inner {
    grid-template-columns: 1fr;
    gap: 40px;
  }

  .pub-hero-right {
    max-width: 520px;
    margin: 0 auto;
    width: 100%;
  }

  .card-grid-4 {
    grid-template-columns: repeat(2, minmax(0, 1fr));
  }

  .footer-grid {
    grid-template-columns: repeat(2, minmax(0, 1fr));
  }
}

@media (max-width: 900px) {
  .site-nav {
    display: none;
  }

  .site-login {
    display: none;
  }

  .menu-toggle-btn {
    display: inline-flex;
  }

  .card-grid-3 {
    grid-template-columns: repeat(2, minmax(0, 1fr));
  }

  .booking-grid,
  .contact-grid,
  .detail-layout,
  .about-grid,
  .about-grid.reverse {
    grid-template-columns: 1fr;
  }

  .detail-side {
    position: static;
  }

  .about-highlight {
    position: static;
  }

  .cta-banner-inner {
    flex-direction: column;
    align-items: flex-start;
  }
}

@media (max-width: 768px) {
  .stats-bar {
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: 24px 0;
  }

  .stat-item:nth-child(3) {
    border-left: 0;
  }

  .facilities-grid {
    grid-template-columns: 1fr;
  }
}

@media (max-width: 640px) {
  .pub-section,
  .pub-alt-section {
    padding: 56px 0;
  }

  .mobile-nav {
    inset: 64px 0 0 0;
  }

  .header-actions .btn-sm {
    padding: 9px 14px;
    font-size: 14px;
  }

  .brand-name {
    font-size: 18px;
  }

  .pub-hero {
    padding: 48px 0;
  }

  .card-grid-2,
  .card-grid-3 {
    grid-template-columns: 1fr;
  }

  .contact-info {
    grid-template-columns: 1fr;
  }

  .form-grid.two {
    grid-template-columns: 1fr;
  }

  .footer-grid {
    grid-template-columns: 1fr;
    gap: 32px;
  }

  .success-details {
    grid-template-columns: 1fr;
  }

  .hero-illustration {
    aspect-ratio: 4 / 4;
  }

  .detail-meta {
    flex-direction: column;
    gap: 12px;
  }

  .cta-actions {
    width: 100%;
  }

  .cta-actions .btn {
    flex: 1;
  }
}

@media (max-width: 480px) {
  .page-hero {
    padding-top: 48px;
  }

  .stats-bar {
    grid-template-columns: 1fr 1fr;
  }

  .stat-item {
    padding: 8px;
  }
}

@media (prefers-reduced-motion: reduce) {
  .hero-spark {
    animation: none;
  }

  .pub-card:hover {
    transform: none;
  }
}

